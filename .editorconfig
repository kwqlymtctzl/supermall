# 表明是最顶层的配置文件，发现设为true时，才会停止查找.editorconfig文件。
root = true

# 通配符
# * 匹配除/之外的任意字符串
# ** 匹配任意字符串
# ? 匹配任意单个字符
# [name] 匹配name字符
# [!name] 匹配非name字符
# {s1,s2,s3} 匹配任意给定的字符串(since 0.11.0)
[*]
# 编码，latin1、utf-8、utf-8-bom、utf-16be和utf-16le，不建议使用utf-8-bom。
charset = utf-8
#  设置缩进风格，tab或者空格。tab是hard tabs，space为soft tabs。
indent_style = space
# 缩进的宽度，即列数，整数。如果indent_style为tab,则此属性默认为tab_width。
indent_size = 2
#  换行符，lf、cr和crlf
end_of_line = lf
# 设为true表明使文件以一个空白行结尾
insert_final_newline = true
# 设为true表示会除去换行行首的任意空白字符。
trim_trailing_whitespace = true

# 还有一个属性没有这里没有设置，总共有8个属性：tab_width只有indent_style = tab才会有
# tab_width : 设置tab的列数。默认是indent_size。